import { FileText } from "lucide-react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function LeasesPage() {
  return <PlaceholderPage title="Baux" icon={FileText} />;
}