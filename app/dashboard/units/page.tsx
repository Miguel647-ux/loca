import { DoorOpen } from "lucide-react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function UnitsPage() {
  return <PlaceholderPage title="Logements" icon={DoorOpen} />;
}